# EstimateTrace v0.1.4

發行日期：2026-07-30

正式環境：<https://estimate-trace.vercel.app>

發行來源：Git annotated tag `v0.1.4`

## 發行識別

- Application version：`0.1.4`
- Schema version：`1.0.0`
- Model version：`bottom-up-1.0.0`
- Parameter set：`public-demo-zh-tw@1.0.1`
- Vercel project：`estimate-trace`
- Vercel runtime：Node.js `24.x`
- Package manager：pnpm `10.34.5`

## 表格排版

- 公式與定義頁及估算結果頁的所有表頭與內容統一靠左對齊。
- 移除估算工作區針對資料列標題與最後一欄的靠右例外，避免同一張表格混用左右
  對齊。
- 新增瀏覽器回歸測試，檢查直接載入公式與定義頁、估算結果頁，以及從估算結果
  導覽回公式與定義頁後的實際計算樣式。

## 相容性、安全性與隱私

- 未變更 dependency、估算公式、數值參數、stable snapshot text 或計算結果。
- Schema、model 與 parameter set version 維持不變；既有案件不需要 migration。
- 案件資料仍只保存在瀏覽器或記憶體中；未新增資料上傳、Route Handler、Server
  Action、analytics 或 third-party tracker。

## 驗證證據

- Formatting、ESLint、TypeScript、Vitest 與 Next.js production build：passed。
- Vitest：14 files／95 tests passed。
- Playwright：desktop／mobile 共 18 tests；表格對齊、既有核心流程與
  accessibility 均 passed。
- Vercel production deployment、正式網址 smoke test 與 runtime error scan
  由發行流程在 tag／push 後執行，結果記錄於發行交接。
