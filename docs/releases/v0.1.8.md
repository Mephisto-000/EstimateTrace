# EstimateTrace v0.1.8

更新日期：2026-08-02

狀態：尚未部署或推送；正式環境仍是前一版。

發行來源：本機 release candidate；待 GitHub 與 Vercel 部署後，再建立對應的 Git annotated tag。

## 發行識別

- Application version：`0.1.8`
- Schema version：`1.0.0`
- Model version：`bottom-up-1.0.0`
- Parameter set：`public-demo-zh-tw@1.0.1`
- Package manager：pnpm `10.34.5`

## 文案調整

- 重新檢查首頁、關於、隱私、範例、建立估算、案件清單、結果與公式說明頁的繁體中文文案。
- 將冗長或偏學術的敘述改為較直接、好懂的說法；必要術語（例如 P50、P80、JSON、localStorage）維持不變。
- 公式與定義頁保留精確的名詞對照和數學內容，並改為先說明用途，再補充計算細節。

## 不變項目

- 估算公式、數值參數、schema、model、案件格式和隱私邊界均未變更。
- 案件內容仍只在瀏覽器處理與保存；未新增 API、Server Action、analytics 或第三方 tracker。

## 驗證證據

- Formatting、ESLint、TypeScript、Vitest（103 tests）與 Next.js production build：通過。
- Playwright desktop／mobile（26 tests）：通過，包含 keyboard、200% reflow、accessibility、估算流程、匯入匯出與資料不外傳檢查。
- `pnpm audit --audit-level=high`：通過，沒有已知弱點。
- 尚未對 v0.1.8 執行 Production response headers、runtime error 或 Network 的實際部署驗證；部署前必須完成這些檢查並更新本文件。
